<!doctype html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Mi Sesión</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-4">
        <% if (locals.casa && casa.username) { %>
            <h1>¡Hola, <%= casa.username %>!</h1>
            <p>Estos son los datos de tu propiedad:</p>

            <h2 class="mt-4">Tus datos</h2>
            <table class="table table-bordered mb-4">
                <thead class="table-dark">
                    <tr><th scope="col">Campo</th><th scope="col">Valor</th></tr>
                </thead>
                <tbody>
                    <tr><th scope="row">ID</th><td><%= casa.id %></td></tr>
                    <tr><th scope="row">Nombre</th><td><%= casa.nombre %></td></tr>
                    <tr><th scope="row">Valor</th><td>$<%= casa.valor %></td></tr>
                    <tr><th scope="row">Imagen</th><td><img src="<%= casa.imagenURL %>" alt="Imagen de <%= casa.nombre %>" width="100" class="img-thumbnail"></td></tr>
                    <tr><th scope="row">Propietario</th><td><%= casa.username %></td></tr>
                </tbody>
            </table>
            
            <a href="/auth/vista-todas" class="btn btn-primary">Ver el resto de casas</a>
            <a href="/auth/logout" class="btn btn-danger">Logout</a>

        <% } else { %>
            <h2>¡No tienes sesión iniciada!</h2>
            <a href="/auth/login-vista" class="btn btn-primary">Ir a Login</a>
        <% } %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>